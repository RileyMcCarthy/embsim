<!-- Plan of record for the board engine: every netlist part is a node in one pipeline. Line references are against the tree at the time of writing (branch feat/p2-qemu-node, 2026-09-23). -->

# Plan: every component a node

## 1. The model

Every fitted part contributes to the one quasi-static nodal solve that already exists (`board/src/cluster.rs` `QuasiStaticMna`: Norton-stamped Thevenin sources, dense elimination; R1, R16). Resistors, closed switch poles and on-state channels are conductances; rails are declared terminals — a source referenced to the regulator's own ground pin, entering every dependent solve as a constant; pads are Thevenin sources at the device's real strength; diodes, LEDs, BJTs, FETs and the CCR are piecewise-linear elements whose region is chosen by a bounded, ordered flip loop inside one solve, cold-started every time; capacitors are single-pole closed forms. A solve runs only when an input to a cluster changes and yields the operating point at that instant. Time enters in exactly one way: a node with capacitance publishes `Settling { v0, v_inf, tau, t0_ns, level }`, analog senses evaluate v(now) at read time, and the instants at which a digital sense would flip and at which the exponential is within one quantum of v∞ are computed in closed form and armed on the wheel as integer nanoseconds. Nothing is integrated per tick (R8, R11). Deliberately not SPICE: no timestep, no Newton on exponential laws, no inductor dynamics (L stays a DC short), no multi-pole transients (one pole per resistive block, stated), no ground the harness did not declare (R18), no parasitics the netlist or scenario does not name. The `ClusterSolver` seam remains the escape hatch (R21); the decision record's revisit trigger is not pulled.

One more rule, from the user, that shapes everything below: **there is one pipeline** — netlist part → registry class → node. No stub tier, no facade-only part, no allow-list. A part is a node whose class has behaviour, or the board refuses to build naming the part and its value. §3 says what that deletes.

## 2. Taxonomy

| Class | In the model | State | Set from | Runtime cost |
|---|---|---|---|---|
| Resistor | conductance edge (today, `system.rs:824-846`). `Pulled(level, ohms)` carries the **winning source's series path** (rule 2), not `edge_total_ohms` (`engine.rs:1236`, the 82 200 Ω artefact). No R_th solve on the fast path: the true R_th comes from `thevenin_at(node)` only inside an already-escalated solve (a reviewer is right that no non-test consumer reads the payload and that an R_th keyed on every slot impedance would re-deliver senses the ROM boot keeps stable today) | ohms, mutable via `SetEdge` (phase 7 only) | value; tolerant first-token parser (62/66 EC32 caps and all four inductors parse to `None` today, `registry.rs:435-485`) | 0 on the fast path |
| Capacitor | annotation on its node, **never a union-find edge** (that grows the EC32 GND/VDD cluster 8→26 nets). Cap to a terminal = per-node pole, τ = R_th·C. Both ends on terminals (decoupling, 55 EC32 caps) = static, skipped. Parallel caps lump. A **symmetric differential pair** (two nodes of equal R_th sharing a cross-cap, the ADC filter shape) decomposes exactly into two single poles, τ_cm = R·C_cm and τ_diff = R·(C_cm + 2·C_d), and is modelled as such. Any other second cap in one block, or a cap on a node that hosts a PWL element or a non-terminal PWL edge, raises `Finding::MultiPoleBlock` and stays DC-open (declaration order) — the PWL-on-a-settling-node case is the back door to transient device simulation and is closed by rule, not by code | per node `{v0, v_inf, tau, t0_ns, level}` + solve generation, engine-side | value; `Harness::capacitor(net, pf)`; `PinDecl::capacitance_pf` opt-in | one `ln_q` per capacitive node per solve; ≤ 2 wheel entries per capacitive node per solve |
| Inductor | DC short (unchanged) | — | — | 0 |
| Diode / LED | PWL: off = `GMIN` leakage (1 GΩ, so every element terminal stays source-reachable and every region test is evaluable — INFINITY would leave the far side Floating and the test with no operand); on = Vf source + r_d edge. LED `lit` = branch current ≥ I_on, via `BuiltSystem::branch_current("D3")` | region bit | part → `pwl_library` with provenance keyed on the netlist's **Manufacturer_Part_Number** (`LTST-C190KGKT` for the 21 Edge LEDs whose value is the bare `LED`; `White` for D601/D602 from the vendor BOM; `SS36`), else the value; no resolvable Vf = build error naming the part, like any other unmodelled part | ≤ 2N solves in its cluster, only when a dirty pass flips a region |
| FET / BJT (Q1, U3, U401) | PWL 3-terminal **with the body diode** (P-FET: D→S) as a PWL branch: BJT = b-e diode + c-e switch when I_b ≥ I_load/hFE(min) from the spec (an under-driven base reads as a sagging collector, not a clean switch); FET d-s = R_ds(on) when V_gs beyond V_th, else GMIN. A region test on a genuinely Floating control terminal evaluates as off. Control pin joins by a GMIN membership edge (gate in-cluster, no cross-cluster dirty) | region bits | `register_pwl(key, PwlSpec)` | as diode |
| Switch / jumper | `PartClass::Switch { poles: Vec<{a, b, state}> }` by **pin id**; a closed pole is a **build-time identity union** (`pin_short` semantics, honouring `detached`), an open pole nothing — no resolver change, no ∞ Ω edge, no `SetEdge`. Jumpers are one-pole switches (`Scenario::jumper` stays as an alias of `Scenario::switch`); 3-pin jumpers = two poles, fixing JP1's latent throw-to-throw short (`system.rs:1071-1078`). Auto tier for 2-pin `SW_*` (SW1, `SW_Push`). S301's `<n>_ON/<n>_OFF` pairing is a registry declaration — the netlist carries none. J101's solder link is a one-pole switch, open. Live toggling is cut: nothing on any board toggles a switch after start (`grep SW1 board/tests` hits only pin tables), the QEMU node cannot act on RESN anyway (`ffi.rs` has no reset entry), and identity union is what the ROM boot relies on today | pole states at build | `register_switch`, `Scenario::switch(ref, pole, state)` | 0 |
| Connector | `Boundary` node (unchanged, `system.rs:888`): pins are pass-through terminals the harness attaches to; an unattached pin is open. EC32 J203, J301 (+ card model), Edge J1–J24, DS2 J1–J5 | — | auto (`Conn`/`Screw_Terminal`/`J`/`P` prefix) or `register_boundary` | 0 |
| Test point | `Probe` node: one pin, senses, never drives; `BuiltSystem::probe("TP3")` reads it. No board carries one today; the class exists so `TP` never means "absent" | — | auto (`TestPoint`/`TP`) | 0 |
| Mechanical | `Mechanical` node: pads recorded, nothing electrical; a pad sharing a net with a driver raises a finding. Edge H5–H8 (`MountingHole_Pad`), EC32 J701/J702 (`Mounting Hole Vss`), `PCB` (no nodes), `NC_Net` (one layout node) — the last three registered by value, since the transcribed netlist has no libsource and their prefixes match no table entry | — | auto (`MountingHole`/`Logo`/`Fiducial`/`H`/`MK`) or `register_mechanical(value)` | 0 |
| Crystal / oscillator | `Oscillator` node: VCC/GND PowerIn; OUT is `StreamRole::PulseSource` publishing the frequency parsed from the value (`TG2520SMN 20.0000M-…` → 20 MHz) **as a rate** when VCC is up — the representation a step clock already uses, one event, zero per-edge cost (R7). Rate routing crosses a capacitor at 0 Ω (AC coupling: X100.OUT → C132 → U101.2A on the EC32); DC resolution keeps it open | rate | value | one publish |
| Logic gate (74LVC2G04 ×2, SN74LVC1G14 ×21) | `LogicGate` node: A sensed, Y driven at datasheet R_out with t_pd as a scheduled instant (cited); Schmitt inputs use the datasheet hysteresis. A channel whose input carries a **rate** is in rate mode: it re-publishes the rate on Y (PulseSink → PulseSource) and drives Y at the rate's average (VCC·duty) through R_out, level sense ignored — so U101's self-biased stage (R101 100 kΩ from 2Y to 2A) solves to its mid-rail fixed point in one pass with no sense→drive iteration | last level / rate | datasheet | one drive per input transition |
| Regulator / rail | `Rail` node: IN/EN Analog senses; OUT is a **declared terminal** (rule 1) driving `{V(ref) + v_set, z_out}` from t_up = t(IN ≥ UVLO ∧ EN) + t_SS (datasheet soft-start, a scheduled wake — the ramp shape is not modelled, the step is a real instant); down = **released** (a real buck/LDO output is high-Z, so a bench strap onto a down rail sources it instead of fighting it) plus `Finding::RailDown { part, reason }` at build; a 0 V source only for parts with an active output discharge, cited. v_set: from the value suffix for fixed parts (`XL1509-5V`, `XL1509-3.3V`, `LDO 300mA, 3.3V`), or from the FB divider the netlist carries, read once at attach through a build-time topology query `ComponentNetIo::resistors_at(pin)` — `V_FB·(1 + R_top/R_bot)`, 0.8 V·(1 + 13.3/10.5) = 1.813 V for U402 (Common_VDD), 0.8 V·(1 + 37.4/10.5) = 3.649 V for U403 (Common_LDOin) — so two `DCDC 3A SOT563` with one registry key come out different, as they are. `ref` = the part's own GND pin; unpinned ref root → Floating + `Finding::UnreferencedDomain`. Isolated DC/DC (UCC12040): VISO **and** GNDS are terminals; a domain nothing ties is pinned at 0 V of its own frame with the finding, so the choice is visible. Current limit is cut: a sagging rail is not a terminal, and a non-terminal rail unions every load into exactly the 26/47-net cluster rule 1 exists to prevent. z_out is recorded for the I-V port, not solved. NaN (`system.rs:1130`) has no survivor: every PowerOut is a modelled rail | up/down | datasheet v_set, z_out, UVLO, t_SS | one drive + one wake per state change |
| Ground | not implicit (R18): the bench return is a harness `power(0V)` terminal; `net_stuck GND 0` stays valid scenario | — | harness | 0 |
| Supervisor (STM1061) | VCC Analog sense on **Common_VDD** (U404 VCC is the core rail in `p2_ec32mb.net`, not VIO); ~OUT `DigitalOut` idle Released; a **comparator with hysteresis** — sinks 0 V @ R_ol while V(VCC) < V_TH (1.6 V), releases at V_TH + V_HYST, both cited from the STM1061 electrical characteristics. No t_rec: the STM1061 is a voltage detector, not a reset supervisor (a reviewer is right; the draft's `t_rec` had no provenance and the part has none) | 1 bit | datasheet | one drive per crossing |
| Digital IC pin | `DigitalOut`/`DigitalBidir` at datasheet impedance (already per drive, `engine.rs:665-690`) | last drive | PinDecl | one drive per input transition |
| Open-drain pin | not a new PinKind: released `DigitalOut` + resistor to a terminal (R19, `vo2631.rs:41-50`). `PinDecl::idle: Option<TheveninDrive>` (`None` = Released, `Some` = any static level and impedance) removes the release-in-attach ritual for **static** idle drives; the second build pass **stays** for the rest (§5). Native firmware: `GpioChannelConfig.open_drain` (sink or release, never push-pull) | — | PinDecl / HAL table | 0 |
| Memory (PSRAM ×4) | `Psram` model (APS6404L QSPI, `models/src/psram.rs`): CE# sensed, SIO released while deselected, the command set answered from an array — the SPI NOR model's shift engine parameterised. The ROM and firmware never touch it; the user's rule says that is not a reason for it to be a facade | array + phase | datasheet | one drive per SIO transition |
| Opto (6N137, VO2631 ×4) | one `Opto` model: LED = PWL branch owned by the part (the transducer primitive, BOARD_ENGINE.md:225), output = open-collector sink while I_LED ≥ I_TH and VCC up (the VO2631 model's rule) | region + 1 bit | datasheet | as diode |
| Analog front end | Analog senses unchanged; populated filter caps → Settling (differential-pair case); REFP/REFN read for ratiometric codes; bridge legs = four edges set by the plant via `SetEdge` (phase 7) | edge values | Config / plant | one value-only edge update per sample |
| MCU node (P2) | `P2Package<Core>`: the package (64 pads, VDD/VIO PowerIn, RESN sensed, XI a **PulseSink** whose delivered rate is the crystal the PLL multiplies — replacing `with_crystal_hz`, XO a released output) around a core: QEMU (`p2-qemu`), the ISS (`SIL/p2iss`), or the native MCU (`McuComponent`, today's `P2EdgeModule` minus its facade). Pad = Thevenin from WRPIN bits 13:11 (OUT=1) / 10:8 (OUT=0): FAST → `P2_FAST_OHMS` (named 25 Ω placeholder with a provenance TODO); 1K5/15K/150K → R; FLOAT → None. The current-source modes (1MA/100UA/10UA) are not mapped — no caller, and a V/I resistor approximation is an invention. **IN reflects the net for every pad whose published drive is None or ≥ `WEAK_DRIVE_OHMS`**; a FAST pad keeps reading its own OUT (what p2core does, `board.rs:482-484`, and what keeps the 60 000-state differential identical). A **START gate** replaces "pads release when the rail is down": the node holds its first wake until VDD, the boot banks' VIO and RESN read up, then runs; clock segment 0 starts at that instant. A rail dropping mid-run is `Finding::BrownoutWithoutReset`, never a pad change, until the vtable has a reset entry | `published: [Option<Option<TheveninDrive>>; 64]` | WRPIN | one drive per pad change |

Every pin is also an I-V port: `PinHandle::sense_current()` / `ComponentNetIo::on_branch(pin, cb)` report the current into the pin from the same solve. It is an Analog-sense-equivalent bench instrument: subscribing one escalates its cluster (only an escalated cluster has node voltages), and it is refused on terminal pins, whose current spans clusters.

### Three rules the taxonomy rests on

**1. Terminals are declared, and they are cluster boundaries.** A terminal is a root held by a *declared* source — a `PowerOut` pin, a harness `power(V)` endpoint, a `net_stuck` — never a root inferred from a drive's impedance (`PinHandle::set_drive` accepts any impedance per call, `component.rs:386-400`, so an impedance-keyed boundary would flip mid-run while membership is fixed at build). Each terminal is its own one-node cluster whose state is assigned once (two disagreeing terminal sources on it → Contention, once, not per dependent), and `build_topology` records a `terminal → dependent clusters` fan-out that `set_drive`/`dirty_scope` consult when a rail changes — so `resolve_dirty == full pass` survives, and the incremental-vs-full property test (`engine.rs:4877-4926`) gains random terminal drive changes as phase 4's first commit. Edges and caps touch a terminal, nothing unions through it, its voltage enters the solve as a constant. This is Dirichlet elimination for terminals, which `cluster.rs:52-64` rejected for *all* ideal sources; the revision is stated there: it is safe because a terminal's own contention is decided before any dependent solve runs, and it is gated on `nominal_analog_cluster` and `net_stuck_shared_node` passing at 1 µV without re-blessing. What the rule prevents: with D/LED as edges and no boundary, `mad_edge.net` collapses +3.3V, +5V, GND and all 21 LED chains into one **47-net** cluster — the chains are inverter Y → 220 Ω → anode → cathode on GND, so it is GND that grows — and any Analog sense in it force-escalates every P-pin edge (`engine.rs:1191-1197`).

**2. Source-strength projection, in one form.** Today two driving slots on one root with different levels are Contention regardless of impedance (`levels_of`, `engine.rs:1017-1029`, `:1219-1224`), so a P2 pad publishing `{3.3 V, 15 kΩ}` against any sink is Contention. The replacement ranks every source reaching a root by **total ohms** = slot impedance + series path from the cached `dist` matrix (a terminal: path only). A source with total ≥ `WEAK_DRIVE_OHMS = STREAM_COLLAPSE_THRESHOLD` (1 kΩ) is a *pull* and never contends. Among the rest: a source ≥ `ESCALATION_IMPEDANCE_RATIO` (10×) the strongest loses — the state is the strongest's, and because it lost through less than 1 kΩ it is a fight, so `Finding::Contention` names both (the flash-vs-card case on P58: the card reaches through 25 + 240 = 265 Ω ≥ 250, node ≈ 3.0 V, reads the flash's level, finding raised — the draft's "Contention" was wrong and the reviewer right). Disagreeing sources within 10× **solve** (m ≤ 2 in every case on these boards), publish the divided voltage, and project it through the dead band `V_IL/V_IH = 0.8/2.0 V` (JESD8-7A LVCMOS, a net-level constant until a PinDecl carries its own): inside the band → Contention + `AmbiguousLevel`; outside → Analog(v) + `Finding::Contention`. Two 25 Ω push-pulls still sit at 1.65 V, inside the band, so every existing Contention holds; a driver against a terminal on its own root stays Analog(rail) as today (the firmware must *see* a short-to-rail, not hold its last level) and gains the finding it lacks. Otherwise the winner sets the state: a strong slot on the root → `Driven(level)`; a terminal on the root → `Analog(v)` (`an_injected_short_outvotes_a_driver_reached_through_resistance` keeps its precedence); anything else → `Pulled(level, ohms)` where ohms is the winner's series path, and a *weak* slot's own impedance is part of that path — a 15 kΩ pad is a resistor to its rail, a 25 Ω pad is a driver. So the 47 Ω single edge stays `Pulled(_, 47)` bit-identical (`a_driven_level_crosses_a_series_resistor`, `engine.rs:3672-3706`), the lone I2C pad reads `Pulled(High, 15000)`, and the unstuck-GND chain reports the path that wins, not 82 200. Escalation trigger 2 (`engine.rs:1186-1197`, both levels present anywhere in the cluster) retires in favour of this ranking.

**3. Membership is fixed at build; the only mutable conductance is the plant's.** Switch poles and jumpers are build-time identity unions (rule above). The one runtime-mutable edge is the bridge leg the plant sets through `Command::SetEdge { edge, ohms }` — value-only, never a `topology_version` bump, refreshing that cluster's `dist` locally — and it lands in phase 7 with its first real consumer. The invariant that wins is "clusters are electrically independent, dirty scope is exact" (`engine.rs:949-958`); there is no epoch machinery and `subscribe_topology` (`engine.rs:2915-2920`) stays the deferred seam it is.

## 3. One pipeline

A netlist part is **either** a node whose class has behaviour **or** the board refuses to build with `RegistryError::UnknownPart { reference, part, value }` (the error gains `value` — on the transcribed EC32 netlist `part` is empty and the message would name nothing). The pipeline is `classify()` → `PartClass` → node; the classes are exactly the taxonomy rows: `Passive`, `Switch`, `Pwl`, `Boundary`, `Probe`, `Mechanical`, `Registered` (a model constructor: `Rail`, `Oscillator`, `LogicGate`, `Supervisor`, `Opto`, `Psram`, `P2Package`, and the models that exist). A connector is a pass-through node, a test point a probe node, a mounting hole or board outline an explicit `Mechanical` node, an IC nobody has modelled a build error. There is no allow-list and no "declare its outputs as inputs so it cannot contend" rule.

**Deleted**, with what replaces each:

| Deleted | Replaced by |
|---|---|
| `boards/src/stub.rs` — `StubPart`, `register_stub`, `dig_in/dig_out/analog/pwr_in/pwr_out/passive/nc`; `pub mod stub` in `boards/src/lib.rs` | the pin helpers become `PinDecl::{digital_in, digital_out, analog, power_in, power_out, passive}` constructors in `component.rs` (a model still writes its table); `StubPart` has no replacement |
| `boards/src/ec32mb.rs` — `STUB_REFS` (:74) and `EC32MB_STUB_REFS` (:188, the same list twice), `stub_registry()` (:191-213), the nine stub pin tables | `Ec32mb::registry()` registers classes: `register_switch`, `register_pwl`, rails, `Oscillator`, `LogicGate`, `Supervisor`, `Psram`, `register_mechanical` for `PCB for P2 EC Module`, `Layout node`, `Mounting Hole Vss` |
| `board/tests/machine_parts/mod.rs` — its own `StubPart`/`register_stub` (:163-186), `EC32MB_STUB_REFS` (:1112), 22 `register_stub` lines (:1123-1131, :1394-1406), the 22 stub pin tables, `P2EdgeModule` (:905-990) | the same class registrations; `P2Package::native(McuComponent)` for the tests that bridge a native serial channel |
| `boards/tests/ec32mb.rs` `P2Slot` ("a processor-shaped placeholder … no behaviour") | `P2Package::held_in_reset()` — the package with its core in reset: every pad released, rails and RESN sensed, XI accepting the rate; the state any P2 is in before it runs |
| `board/src/registry.rs` — `Classification::Stubbed` (never constructed), `Classification::Ignored`, `ExpansionHook`/`register_expansion`/`expansions` (no caller), the tier-3 "explicit stub list is the only escape" text | `Classification::{Mechanical, Probe, Switch}`; tier 3 is the error |
| `board/src/board.rs` — `PartClass::Stubbed`, `Board::from_netlist_with_stubs`, the `stub_refs` arm (:155-159), the `Ignored => continue` arm (:135) | `Board::from_netlist` is the one constructor; `PartClass::{Mechanical, Probe, Switch, Pwl}`; every netlist part has a record |
| `board/src/system.rs:888` `PartClass::Boundary \| PartClass::Stubbed => {}` | `Boundary` alone; `Mechanical`/`Probe` arms |
| `p2-qemu/src/lib.rs` `with_crystal_hz`, `DEFAULT_CRYSTAL_HZ`, `output_level` (:405-414) | XI `PulseSink` delivery sets `crystal_hz`; `pad_drive(pin)` |
| `SIL/p2iss/src/lib.rs:946-951` P59 fiat | a `Pull` on P59 in `rom_boot_net.rs`/`rom_serial_net.rs` (the harnesses there have a strap on P61 and a MISO pull-up, no P59 source — a reviewer is right that deleting the fiat alone strands them), the EC32 path uses S301 |
| the stub doc rule "an output nothing reads is declared `dig_in`" and the tests that encode it (§4) | outputs are outputs |

**How the EC32MB gets every part classified** (114 parts; today 22 registered of which 21 are stubs in the test registry and 20 in `boards`):

| Part(s) | Today | Class |
|---|---|---|
| U100 `P2X8C4M64P` | slot, stub-shaped in `boards/tests` | `P2Package<Core>`; an empty slot is a build error (already) |
| X100 TCXO | stub, OUT a sense | `Oscillator`, 20 MHz from the value |
| U101, U601 `74LVC2G04GW,125` | stub, Y a sense | `LogicGate` (U101 in rate mode; U601 drives the LED cathodes) |
| U301 flash | model in `boards`, stub in tests | model everywhere |
| U302–U305 `PSRAM 64Mbit` | stub | `Psram` |
| U401 `P Mosfet 30V 8A` | stub + `pin_short` | `Pwl` P-FET with body diode |
| U402, U403 `DCDC 3A SOT563` | stub, SW a NaN PowerOut | `Rail` (buck, v_set from the FB divider) |
| U404 `Voltage Detector 1.6V` | stub, ~OUT a sense | `Supervisor` |
| U501–U508 `LDO 300mA, 3.3V` | stub, OUT a NaN PowerOut | `Rail` (LDO, EN sensed) |
| S301 `DIP Switch 4 way` | stub, 8 passive pins + `pin_short` | `Switch`, 4 poles |
| J101 `Solder Link Pads` | boundary | `Switch`, 1 pole, open |
| J203, J301 | boundary | `Boundary` (J301 + card model) |
| J701, J702 `Mounting Hole Vss` | boundary | `Mechanical` (registered by value) |
| `PCB`, `NC_Net` | `STUB_REFS` escape | `Mechanical` (registered by value) |
| D601, D602 `White` | DC-open | `Pwl` LED |
| R ×15, C ×66, L ×2 | auto | `Passive` (caps as annotations) |

**The Edge board** (168 parts; 49 registered, 46 of them stubs):

| Part(s) | Today | Class |
|---|---|---|
| U24, U25, IC5 | models | unchanged |
| IC1, IC2, IC14, IC15, IC16 ISO67xx | stubs | the existing `iso67xx` model, promoted (`isolation_bridge.rs:244` already does this per test) |
| IC3, IC4 `UCC12040DVER` | stub, VISO/GNDS NaN PowerOut | `Rail` (isolated, VISO + GNDS terminals) |
| IC6–IC13 `NSI50010YT1G` | stub / chain model | `Pwl` CCR element (the `nsi50010` model's curve, as an element) |
| U4 `6N137`, U5–U8 `VO2631` | stubs / `vo2631` model | `Opto` |
| U9–U34 `SN74LVC1G14DBV` ×21 | stub, Y a sense | `LogicGate` (Schmitt) |
| U1, U2 `XL1509` | stub | `Rail` (fixed, from `-5V`/`-3.3V`) |
| U3 `APM4953` | stub + `pin_short` | `Pwl` P-FET with body diode |
| Q1 `2N3904` | stub / `npn_switch` model | `Pwl` BJT |
| SW1 `SW_Push` | stub, 2 passive pins | `Switch` (auto, `SW_` prefix), open |
| JP1 `Jumper_3_Open`; JP2–JP5 | jumpers | `Switch` (two poles; one pole) |
| J1–J24 | boundary | `Boundary` |
| H5–H8 `MountingHole_Pad` | ignored (absent) | `Mechanical` |
| D1, D2 `SS36`; D3–D25 `LED` | DC-open | `Pwl` diode; `Pwl` LED keyed on `LTST-C190KGKT` |

The DS2 addon has no stub today (U1 is the ADS122U04 model; the rest are auto) and changes only in naming. The census tests change with the pipeline: `ec32mb_module` asserts 20 model nodes + 2 primitive nodes (S301, U401), `edgeboard` 38 + 11 (SW1, Q1, U3, IC6–IC13) — not the 47/20 a reviewer estimated, because the eight CCRs become elements too; `component_refs()` becomes `nodes()` returning `(ref, class)` so both counts are asserted. A `stub_count` census is a CI gate from phase 1: it must never rise, and it is 0 on all three boards when `stub.rs` is deleted in phase 4.

## 4. Interfaces

**I2C.** No I2C device exists on any MaD board (NavKey header J19 only, no pull-ups in `mad_edge.net`; `jm_i2c` uncalled; `peripherals/src/i2c.rs` a no-op). A reviewer is right that this gives phase 6 no board consumer; it is kept because it is the question this plan answers, as a **bench** fixture. Two things had to be true and were not. First, rule 2: `jm_i2c` PU_15K publishes `{3.3 V, 15 kΩ}` on a pad the driver **never releases** — it drives high through the pull mode (`wrpin pullup` then `drvh`, `jm_i2c.spin2:57-65`) and samples with `testp` — so alone the pad projects `Pulled(High, 15000)`, a slave sinking 0 V @ 30 Ω wins (15 k ≥ 10 × 30) → `Driven(Low)`, no matrix. Second, the node's read: with `sensed()` = `(dir & out) | (!dir & in_ext)` (`p2-qemu/src/lib.rs:398-401`) every ACK would read NAK and a stretching slave would be invisible; the fix in §2 (IN = net for weak or released pads) is what makes the clock-stretch wait `drvh scl … testp scl / if_nc jmp $-1` (`:113-116`) and the ACK sample (`:146-151`) work. Two masters resolve by whichever sinks, and the one reading Low while driving high through 15 kΩ backs off. Rise time only with `Harness::capacitor("SDA", 100)`: 15 kΩ·100 pF crosses 1.5 V at τ·ln(3.3/1.8) = 0.606τ = 910 ns. `bridge_i2c` on native firmware is cut: `jm_i2c` has no caller. New `board/tests/i2c_bus.rs` with a bench `i2c_slave.rs`; no existing test changes.

**SPI (EC32MB).** S301 becomes a switch: `rom_boot_ec32mb.rs:166-172` drops both `pin_short`s for `.switch("EC32.S301", 2/4, Closed)`; SPI_CS is identity-merged with P2_IO61 through the closed pole and reads `Driven(Low)` from P61 with no projection change; `unmodeled_oscillator_and_open_dip_switch_float_their_nets` (`ec32mb_module.rs:556-583`) flips to `Pulled(High/Low, 10500)` by pole, and its XI half to "XI carries a rate". P58 is a three-source node; a flash-vs-card fight is `Driven(flash level)` with `Finding::Contention` (rule 2). Firmware observes nothing new on a healthy board; the ROM boot compares no golden trace.

**UART.** Unchanged by default: no netlist carries signal-line capacitance and pad capacitance is opt-in, so `a_firmware_byte_crosses_the_net_as_edges` (`serial_levels.rs:597-609`) and the `serial_levels` golden hold. With 100 pF declared on TX (τ = 2.5 ns at 25 Ω) the rising edge crosses 1.5 V at 2.5·ln(3.3/1.8) = 1.515 ns and the falling at 2.5·ln(3.3/1.5) = 1.97 ns — **+2/+2 ns** on the integer grid, not the draft's +1.5/+2.0 (a reviewer is right; the phase-5 test asserts instants from the engine's own rounding function); a new test asserts the byte still decodes. The "missing pull-up reads a permanent start bit" bench symptom is reproduced only by `Harness::leak(net, volts, ohms)` — an explicit weak source in scenario, never invented (R2).

**Step/dir + isolator.** STEP stays a rate (R7; 820 k edges/s is measured infeasible). DIR/ENA unchanged. Q1 → PWL NPN: `Net-(Q1-B)` reads Analog(≈0.7) not `Pulled(Low, 43000)` — `the_enable_path_crosses_the_isolator_and_the_transistor` (`isolation_bridge.rs:763-810`) updates, and asserts SC_ENA's *voltage* with the drive's opto input in the harness (series R + LED to its 5 V, bench): I_b = (V_OUTC − 0.7)/43 kΩ ≈ 60–100 µA against a ~10 mA opto load leaves a 2N3904 β-limited, and the model says so rather than reading a clean switch. JP1 gets a real two-pole position. RELAY_EVENT_CEILING=32 unaffected.

**Encoder.** U25 inputs are already Analog; no change. Velocity pacing and propagation delay stay out.

**Analog (ADS122U04).** DNP by default → nothing changes. With R6/R7 + C7..C9 populated (values declared by the test, the netlist says `X`): C7/C9 are the common-mode caps and C8 the differential one — the standard ADC filter, where the differential cap is normally ~10× the others and sets the pole the converter actually sees. The symmetric-pair case gives τ_cm = R·C_cm and τ_diff = R·(C_cm + 2·C_d): with C_cm = 100 nF and C_d = 1 µF, τ_diff = 4.7 kΩ·2.1 µF = 9.87 ms, ~20× the draft's 470 µs (a reviewer is right; the draft dropped the dominant pole). The same applies to C2 across REFP/REFN. RDATA at a conversion instant reads v(now); `rdata_returns_the_conversion_for_the_driven_bridge_differential` gains a 5τ_diff settle and a mid-exponential case; the 1e-6 V asserts get a stated tolerance.

**Power, enable, brownout, reset, clock.** Regulators drive real volts, so bench straps become ideal-vs-rail Contention (as `ds2_regressions.rs:250-276` already shows). Tests that encode stubs, and what each becomes: `bench_straps_clear_the_domains_they_feed` (its `dark` half inverts: U3's body diode lifts V_IN without the FET scenario), `a_bare_board_reports_only_the_domains_that_arrive_over_a_cable`, `the_assembled_machine_leaves_only_the_unplugged_ports_unsourced`, `the_whole_machine_builds_without_contention` — the on-board straps go, J2 `power(12V)/power(0V)` and the servo-side J21 endpoints (a domain the drive supplies) remain; `the_bridged_tx_pin_is_the_only_driver` → `every_driver_is_a_registered_model` (TX, X100, U101, U601, U404); `the_reset_node_is_pulled_up_and_floats_without_the_pull_up` → a POR sequence: `Driven(Low)` while Common_VDD < 1.6 V, `Pulled(High, 10500)` from t_SS(U402) (RESN carries no capacitor, so no RC delay); the two census tests (§3). IC14's orphan GND2 surfaces as `UnreferencedDomain` once ISO67xx reads VCC2−GND2 instead of V(VCC2) (`iso67xx.rs:111-115`). The P2 starts at the START-gate instant; the icount node's clock comes from XI.

## 5. Engine changes (files)

- `board/src/registry.rs`: tolerant `parse_passive_value`; `Classification::{Switch, Mechanical, Probe}` checked in `classify()` before the value lookup (`:262-264`, S301 has no libsource); `register_switch`, `register_pwl`, `register_rail`, `register_mechanical`; auto `SW_*`; `UnknownPart` gains `value`; delete `Stubbed`, `Ignored`, `ExpansionHook`.
- `board/src/board.rs`: `PartClass::{Switch { poles }, Pwl { spec, pins }, Mechanical, Probe}`, pins validated both ways like `validate_facade`; `from_netlist_with_stubs` deleted; `nodes()`.
- `board/src/system.rs`: `Scenario::switch`; `Harness::capacitor`, `Harness::leak`; closed poles → identity unions honouring `detached`; `add_capacitor` (annotation), `add_pwl`; `PowerOut` → terminal + `ref_root`; `ComponentNetIo::resistors_at`. The second build pass (`:483-506`) **stays** and becomes a bounded fixed point: replay recorded attach drives, re-resolve, repeat while they change, bound = declared power-tree depth, `Finding::BuildNotSettled` past it. A reviewer is right that deleting it was wrong: the GPIO bridge drives its power-on level at attach (`mcu.rs:1189-1193`), the ISO67xx drives at its *sensed* rail (`iso67xx.rs:729-741`), and the EC32 tree is J203 → U401 → U402.IN → Common_LDOin → U501..U508 EN/IN, three senses deep. Rails with t_SS > 0 are down in the build snapshot — honestly, since no wake has run — and `powered_module()` becomes build + start + step to t_SS. New invariant, asserted on Edge and EC32MB: the build snapshot equals the live system's state before its first wake.
- `board/src/component.rs`: `PinDecl::idle: Option<TheveninDrive>`, `PinDecl::capacitance_pf`, constructors; `sense_current`, `on_branch`, `edge(a, b) -> EdgeHandle`. `PinKind` untouched.
- `board/src/cluster.rs`: `Cluster { …, pwl, capacitors, edge_ohms }`, `ClusterInputs { sources, terminals, edge_ohms }` — **no regions**: every solve cold-starts with all elements off; `ClusterSolution { node_states, regions, branch_currents }`; terminal stamps; one-at-a-time flip loop; `thevenin_at(node)`; `GMIN`. Trait stays `&self`. The Dirichlet revision stated in the module doc.
- `board/src/engine.rs`: terminals as one-node clusters and the fan-out table in `build_topology` (`:769-871`); rule 2 replacing `levels_of`/contention/trigger 2/state assignment (`:1017-1029`, `:1075-1116`, `:1205-1241`); per-cluster `dynamics`; `TimerTarget::{Crossing, SettleEnd} { cluster, generation }` through `arm`/`fire_due_timers`/`next_virtual_deadline` (`:1962-2432`, `:2768`); `resolve_reference` extended; `Command::SetEdge` in phase 7.
- `board/src/net.rs`: `Settling`, `level_of` over it, `volts_at(state, now)`, `WEAK_DRIVE_OHMS`, `V_IL`/`V_IH`, `ln_q`. `board/src/event_log.rs`: `Crossing`, `SettleEnd`, `RegionFlip`, `EdgeSet`; `Finding::{NonConvergent, MultiPoleBlock, UnreferencedDomain, UndecoupledPowerPin, RailDown, BuildNotSettled, BrownoutWithoutReset}`.
- `board/src/mcu.rs`: `GpioChannelConfig.open_drain`.
- `models/src/`: `regulator.rs`, `supervisor.rs`, `logic_gate.rs`, `oscillator.rs`, `psram.rs`, `opto.rs`, `pwl_library.rs` (provenance per entry), bench `i2c_slave.rs`; the NSI50010/NPN chain trick (`isolation/mod.rs:46-52`) retires into elements and their 6.92 Ω / 100 Ω tunings to the 10× rule are re-derived from datasheets.
- `boards/src/`: `p2.rs` (`P2Package`), `ec32mb.rs` rewritten to classes, `stub.rs` deleted.
- `p2-qemu/src/lib.rs`: `pad_drive(pin)`; `sensed()` per §2; XI `PulseSink`; START gate; `wrpin` routes through `recompute_and_mark`. WRPIN edges are stamped at the executing cog's clock once the vtable's `wrpin` carries a cog (the target is beside this crate in MaD `SIL/qemu-p2/`, and knows the cog at the call); until then they are stamped at `machine_now_ns()` and the caveat is documented. `SIL/p2core/src/board.rs:482-484` takes the same `sensed()` rule; `SIL/p2iss` takes the XI sink and loses its fiat.
- `board/tests/machine_parts/mod.rs`, `boards/tests/ec32mb.rs`: stubs and placeholders replaced by classes.

## 6. Cost model

Solves per event are unchanged: one `resolve_dirty` of the touched cluster per applied drive (`engine.rs:2291-2361`), on the fast path unless two sources within 10× disagree (an m ≤ 2 solve) or an Analog sense is present. Additions are bounded per cluster: rule 2 is an O(sources) ranking per dirty root from cached `dist`; PWL adds ≤ 2N solves only in a dirty cluster containing elements (cold start makes that the bound every time, and at m ≤ 8 it is ~170 multiply-adds per solve); a settle adds one `ln_q` per capacitive node and at most two wheel entries; there is no R_th solve on the fast path. Rails add no solves — terminals are constants — and a rail change costs one fan-out walk.

Cluster size is the decision. Rule 1 keeps every pull-up a 2-node cluster and every LED chain {Y, anode} plus terminals; identity unions keep S301 at zero cost; today's largest clusters (EC32 8 nets across GND/Common_VDD/LDOin, Edge 11 across +3.3V/LED anodes — offline sizing) split at their rails. Decoupling caps contribute nothing. Expected max m ≤ 8 on all three boards; phase 0 measures, phase 4 bounds. On the ROM boot every SPI edge is a 25 Ω pad against 10.5 kΩ to a VIO terminal (10 500 ≥ 1 kΩ: a pull), so no matrix is built today and none after; the projection gains one compare per source pair. Expect the 16.9 k edges within noise of 0.2 s (≈12 µs/edge wall, dominated by QEMU slices and wakes, not the engine). LU caching waits for phase 7 and is dropped if the bench does not justify it. Event ceilings (`isolation_bridge` 32, `pulse_bridge` 64) are rate-independent and untouched because STEP stays a rate.

## 7. Determinism

The crossing is Δ = τ·ln((v∞ − v0)/(v∞ − v_th)), defined only when v_th lies strictly between v0 and v∞ (the (v0 − v_th)/(v∞ − v_th) form in the findings is ln of a negative for a 0→3.3 V step); the settle end is Δ_end = τ·ln(|v∞ − v0| / 1 µV). The log is **embsim's own** `ln_q`: fixed-point over the already-quantized 1 µV / 1 mΩ inputs (table + integer Newton, stated max error 1e-9 relative, no libm) — a reviewer is right that a platform `ln` inside an event instant is the first place a cross-OS ULP would change event *order* rather than a logged value, and the goldens are blessed on macOS and compared on the ubuntu-only leg (`ci.yml:60-66`). τ stays f64 from the quantized R_th and C (no τ_ns rounding: 25 Ω·100 pF is 2.5 ns, not 2 or 3); Δ is computed in f64 from `ln_q`'s result and rounded exactly once: t = t0 + round(Δ_ns). A Δ that rounds to 0 is **not scheduled** (round, not ceil), and the settle-end entry is armed only for nodes with an Analog sense — which is why a pad-scale RC on a 25 Ω driver into 10 pF leaves the bit grid untouched on every SPI edge. `Settling` is published only when the crossing rounds to ≥ 1 ns or an Analog sense exists; otherwise the DC projection is published at once. At t_cross the engine republishes with `level` flipped — a different tuple, so `same_state` delivers; at t_end it republishes the DC projection. `level_of(Settling)` is its `level` field (existing consumers stay correct; a reviewer is right that the draft's "evaluate at read time" could not deliver under `same_state`); `volts_at` is for RC-aware analog consumers. A capacitive node whose every source is released publishes `Analog(v0)` held (memory, no entries, no invented value). Same binary + same inputs → same bits (R12d); the macOS leg joins the determinism job for any fixture that carries a `Crossing` record before phase 5 blesses one. Every armed instant carries the cluster's solve generation and a wheel seq; it fires in (deadline, seq) order after same-instant drives (R11); a drive on a settling cluster re-solves with v0 = v(now), bumps the generation, and the stale entry is dropped on fire. PWL flips are one-at-a-time in declaration order from a cold start, bound 2N, so a pass is a function of (topology, drive table, t) alone — warm-starting from the previous solve made two histories reaching one drive table publish different states and broke `resolve_reference`; on exhaustion the element's nodes publish Floating with `Finding::NonConvergent`, never NaN, never a "last consistent solution". Sources stay slot → power → stuck; no HashMap on the path (D0). The incremental-vs-full test (`engine.rs:4877-4926`) is extended to terminals, Settling and PWL clusters as a hard gate. `rs422_determinism.rs` (`:88-121`, an open free-running divergence) is investigated in phase 0, before the sense→drive cascades that expose it multiply; until it is closed every new model's proving tests run in stepped mode only. Goldens: only four exist (`net_stuck_shared_node`, `nominal_analog_cluster`, `serial_levels`, `wake_ladder`); none carries Pulled ohms, caps or PWL, so every phase's gate is that all four pass **without re-blessing**. New fixtures (rc_step, i2c_bus) get goldens blessed once; CI still fails on a rewrite.

## 8. Phases (smallest first; each with its proving test)

0. **Census, baseline, gates.** `board/tests/cluster_census.rs` prints `topology.clusters[*].roots.len()` and the stub count for EC32MB/Edge/DS2; a `solve_bench` example records ns/solve for m ∈ {2, 4, 8, 11, 47}; the ROM boot counts escalated resolves (expect 0) and records edges/wall as baseline; the rs422 divergence gets a reproduction that fires on demand or a stepped-only rule. Proof: census committed as a fixture.
1. **One pipeline groundwork, value parser, rule 2, idle drives.** Classes `Mechanical`/`Probe`/`Switch`/`Pwl` as types; `Stubbed`/`Ignored`/`stub_refs`/`ExpansionHook` deleted from the registry and board build; `UnknownPart` names the value; `stub_count` becomes a never-rises gate; `PinDecl::idle`; the build fixed point and the build-equals-pre-wake invariant test. Proof: `"4.7uF 6.3V"` → 4.7e-6, `"47uH/3A"` → 47e-6, 66/66 EC32 caps parse; 15 kΩ slot High + 30 Ω slot Low on one root → `Driven(Low)`; 25 Ω vs 100 Ω → Analog(2.64) + finding; 25 vs 25 → Contention as today; a driver Low against `net_stuck` 3.3 V on its root → Analog(3.3) + finding; the 47 Ω edge still `Pulled(_, 47)` bit-identical; the unstuck-GND chain reads the winner's path, not 82 200; the two-sink wired-AND (either Low → `Driven(Low)`, both released → `Pulled(High, 4700)`); `PCB`/`NC_Net`/J701/J702/H5–H8 are `Mechanical` nodes; four goldens unchanged.
2. **Switch, oscillator, gates, memory, P2 package, pads.** S301/SW1/JP1/J101 as switches; `Oscillator` + rate-through-capacitor routing; `LogicGate` for U101, U601 and the 21 SN74LVC1G14; `Psram`; `P2Package` with the XI sink replacing `with_crystal_hz`, `P2EdgeModule` and `P2Slot`; `pad_drive` + the `sensed()` fix in p2-qemu and p2core; ISO67xx promoted on the Edge; the p2iss fiat replaced by a `Pull`. Proof: `ec32mb_module` P59 `Pulled(High, 10500)` with pole 3 closed, `Pulled(Low, 10500)` with pole 4; SPI_CS `Driven(Low)` through pole 2; `rom_boot_ec32mb` boots with `.switch` and no `pin_short`, its clock from the rate on XI (HUBSET to 160 MHz from the delivered 20 MHz); a pad at `{3.3 V, 15 kΩ}` with the net `Driven(Low)` reads 0 via `testp`, and the p2core differential stays 60 000/60 000; `edgeboard` JP1 A–B puts Q1's collector on SC_ENA, B–C puts OUTC; `wrpin(P_HIGH_15K)` on a released net → `Pulled(High, 15000)`, `P_HIGH_FLOAT` → released.
3. **PWL elements + branch current.** Diodes, LEDs, CCRs, FETs with body diodes, the BJT, the opto LED branches; `GMIN`; cold start. Proof: Edge D3 lit with I ≈ (3.3 − Vf)/220 when its inverter output is **high**, anode ≈ 0 V when low; U3/U401 conduct forward (body diode on → V_gs < V_th → channel on → diode off, the start-up sequence) and block reversed, without `pin_short`; Q1 base Analog(0.65–0.75) and SC_ENA's voltage with the opto load; end-switch loop regulated at 10 mA ± 1 %, not 27 mA; non-convergence fixture → `NonConvergent`, never NaN; two histories reaching one drive table publish identical states; incremental == full for PWL.
4. **Terminals, rails, supervisor, isolated domains — and `stub.rs` goes.** Declared terminals with fan-out (the property test extended first); `Rail`, `Supervisor`, UCC12040; `UnreferencedDomain`, `UndecoupledPowerPin` (needs the ref pin, so it lives here, not in phase 0), `RailDown`; the START gate; the I-V port. This phase classifies the last stub users (U402/U403/U404/U501–508, U1/U2/IC3/IC4), so `boards/src/stub.rs`, the test-side copies and `from_netlist_with_stubs` are deleted in it and `stub_count` reads 0. Proof: EC32MB power tree reads Analog(3.3) ± 1 mV on every VIO and Analog(1.813) on Common_VDD from the J203 5 V/0 V fingers with no `net_stuck`, U402 and U403 different from one registry key; `p2_resn_is_sunk_until_common_vdd_crosses_1v6` asserts the t_SS instant; the ROM boot runs from the START gate with its `net_stuck` lines removed; `cluster_census` bounds max m ≤ 8; `bench_rails` shrinks to J2 + J21 and `the_whole_machine_builds_without_contention` still passes; IC14 GND2 → `UnreferencedDomain`; `sense_current` into a Low sink equals (3.3 − v)/R to 1 nA; build snapshot == pre-wake state on both boards; `no_board_contains_a_stub_part` == 0.
5. **Capacitors.** Proof: `cluster_handchecks` RC step reads 63.2 % at t0 + τ to 1 µV and the digital sense fires at exactly `round(τ·ln(3.3/1.8))` ns; a second drive before the crossing cancels it (one `Crossing` record); 25 Ω into 10 pF schedules nothing and the bit-grid test passes with that pad cap declared; 100 pF on TX: the byte decodes, edges at +2/+2 ns; DS2 populated filter settles by 5τ_diff with the pair decomposition; `MultiPoleBlock` on an asymmetric two-cap fixture and on a cap-plus-PWL node; a released TX with 100 pF holds `Analog(v0)`; `Harness::leak` reproduces the permanent-start-bit symptom; incremental == full for Settling; `ln_q` matches libm to 1e-9 on 10⁶ points; the macOS determinism leg green; four goldens unchanged.
6. **I2C bench.** `i2c_bus.rs`: 100 kHz round trip, slave stretches and the master waits, two masters arbitrate, rise at 910 ns with 100 pF; a strength change at the same level re-publishes.
7. **Plant-driven edges; caching only if earned.** `MaDSim/src/wiring.rs` BridgeDrive → four `SetEdge` legs (hand-computed differential to 1 µV), the first mutable conductance; an LU cache per (topology, impedance signature) only if the phase-0 bench shows an escalated cluster on a hot path.

## 9. Non-goals, and the answer

Non-goals: transient/timestep SPICE or ngspice; inductor dynamics; multi-pole RC beyond the symmetric pair (one pole per block, with the finding); Newton or exponential device laws (PWL regions only); regulator current limit; pad Schmitt/hysteresis and ADC modes beyond the ROM seed; P2 current-source pull modes; isolator/receiver propagation delay; thermal and parasitics; STEP as edges; membership changes at runtime; a native `bridge_i2c`; deterministic host-PTY runs; migrating MaD's hand-wired GPIO/encoder/pulse seams (unified-drive first).

**Does it make sense?** Yes — and most of it is already the design: R1 makes a pull-up a resistor, R19 makes open-drain a released drive, R8 already specifies the closed-form RC. "A rail is more like SPICE" is right at exactly the granularity that stays cheap: a rail is a declared terminal referenced to its own ground, entering every neighbouring solve as a constant, which is what keeps every solve at m ≤ 8 and the per-edge budget intact. Your I2C example needs three things the engine lacks, none of them a solver: an impedance-aware fight rule (a 15 kΩ pull against a 30 Ω sink is not contention — today it is), P2 pad pull modes, and a pad that reads the net when it is only pulling it — the last is the one the driver actually depends on, and the reviewer found it. **Where it becomes too complex** is exactly four places, and this plan refuses each: capacitors as union-find edges (8→26-net clusters and multi-pole math), Newton or transient device laws (a timestep, convergence nondeterminism), a capacitor on a PWL node (piecewise transient simulation by the back door), and per-edge step trains. With "every component a node" taken literally — no stub tier, no facade — the price is a handful of small models nobody has needed yet (a PSRAM, an inverter, an oscillator, a regulator, a comparator, an opto), each cheaper than the stub rule that excused it, and the reward is one pipeline: a part is a node with behaviour or the board tells you which part and which value it cannot explain. Held to declared terminals, shunt-C closed forms with a stated one-pole rule, PWL regions cold-started in an ordered bounded loop, and switches as build-time unions, it is four primitives (shunt cap, PWL element, terminal, plant edge), one NetState variant, two timer targets, one projection rule, and stubs replaced by models — not a new simulator. The honest cost is the test churn in §3 and §4: those tests encoded the stubs.
## 10. The node ↔ net interface

Reviewed against the tree, against the physics of every interface on the three boards, and against IBIS 7.2, IEEE 1800/1364 strengths, IEEE 1164, SystemC-AMS, SPICE/MNA and Modelica conservative ports.

**Per instant: one message, `Drive`, three encodings, all through the same sequenced command at the producer's own instant (R1–R5).**

| Encoding | Meaning | Covers |
|---|---|---|
| `Thevenin { volts, ohms }` | a linear port; `ohms = ∞` is normalised to *released* at the slot, never ranked | push-pull pads, sinks (open-drain low = `{0 V, R_on}`, high = released), the P2's 1K5/15K/150K pull modes, rails, straps, stuck-at faults |
| `Current { amps }` | a Norton injection with no shunt: stamped as `rhs += amps`, contributes no reachability (a current source into an open loop leaves the node Floating, with a finding) | the eight NSI50010 current regulators, the P2's 1 mA/100 µA/10 µA pad modes, a regulator's input load — the two places the tree already fakes a current source with a resistor |
| `Periodic { hi, lo: Thevenin, segment: PulseSegment }` | the record's shape (`sil-unified-drive.md`): a declared compression of a Thevenin edge train with the integer `{freq_hz, since, emitted, total}` that keeps the count exact for late joiners | the step clock, forwarded verbatim by the isolator; novel by measurement (820 k edges/s), not by any standard |

No `OpenDrain`/`PushPull` kinds. Open-drain is a behaviour of the node — a sink that releases — and every kind added is an interface variant every consumer must understand; IBIS keeps its `Open_drain` model type only "for backward compatibility" and defines it as "do not use `[Pullup]`".

**Per pin, declared once on `PinDecl` and stamped by the solver, never republished** — IBIS's decomposition:

- `idle: Option<TheveninDrive>` — the power-on drive (`None` = released), which retires the release-at-attach ritual.
- `clamps` — always-stamped shunts to the pin's own supply and ground pins (IBIS `[GND Clamp]`/`[POWER Clamp]`). A pad's *driver* strength is its published Thevenin; a pad that needs a nonlinear driver curve is a PWL element part, not a pin table. Precedence stated so the two never disagree.
- `input: { v_bias, r_in }` — a sense pin's own port (IBIS `Input`): the AM26LV32's 12 kΩ to its bias is the mechanism of its open-input failsafe, a regulator's quiescent load is what holds a blocked V_IN near 0 V; without it those nodes come out of an arbitrary 1 GΩ divider.
- `capacitance_pf`, `thresholds` (`V_IL/V_IH` plus a hysteresis pair, declared relative to the pin's supply or reference pin so a brown-out scales them; JESD8C.01's 0.8/2.0 V as the net-level default), and `can_source` — the one static fact a per-instant message cannot carry. It exists for a build lint, `Finding::OpenDrainWithoutPullUp`: a pin that cannot source, on a net where no other source reaches it through less than `WEAK_DRIVE_OHMS`.
- Nonlinear devices are PWL **branches on the component** — `(pin_a, pin_b, curve, control)` — not per-pin curves to a reference: a diode is a branch between two pins, a FET channel is a D–S branch decided by V(G) − V(S), and the per-pin form is the degenerate case.

**Delivered to a sensing pin:** `Sense { volts: Option<Volts> }`, relative to the pin's declared reference — `None` means no source reaches the node, which is a fact consumers branch on (an open enable on the ISO67xx means *enabled*), not an ambiguous voltage. The digital level is the **receiver's** projection through its own thresholds and hysteresis, chosen by its last level — one compare per delivery, which is what delivery already costs. The policy for a voltage inside the dead band is declared per receiver (`HoldLast` or `Unknown`) with no crate default: `level_of → None` keeps refusing to invent a level, as today. Contention and floating stay distinguishable findings. Current into a pin is an instrument (`sense_current`), not the normal path.

**Resolution** is rule 2: sources reaching a root ranked by total ohms; a pull (≥ 1 kΩ) never contends; ≥ 10× loses with a finding; within 10× an m ≤ 2 solve publishes the divided voltage. Ordinal where Verilog's strength resolution is exact, nodal where Verilog would answer X — the 10× and 1 kΩ are embsim constants, not standard values. A root with exactly one reaching source delivers its open-circuit voltage without a solve, even to an analog sense.

**Sequencing, so nothing regresses:** rule 2 before the pad pull modes are mapped (today a `{3.3 V, 15 kΩ}` pull against any sink is Contention because the fight rule ignores ohms); rails as real terminals (phase 4) before the sense becomes volts-only (every supply gate reads a NaN-sourced rail's `Pulled(High)` today); ∞ Ω normalised at the slot before it can be ranked; the pulse channel folded in per `sil-unified-drive.md` steps 1–4, since today it is a second interface with its own registration, routing and un-sequenced delivery.

**What this is, in the vocabulary of other tools.** SPICE stamps every element as a Norton pair and solves KCL with a timestep; Proteus VSM keeps a strength-coded logic state per digital pin and bridges to SPICE with interface objects carrying an output resistance; Icarus and VHDL resolve by an ordinal strength table with no voltage; Renode and QEMU carry a `bool` or an `int` per line and bytes per bus, with no strength, no resistor and no conflict detection. embsim's interface is SPICE's linearisation at event instants with Proteus's ordinal shortcut where it is exact — which is why it can see the bugs the transaction-level tools cannot (a fought line, a missing pull-up, a floating input, a stretched clock) at a per-edge cost the timestep tools cannot afford.
