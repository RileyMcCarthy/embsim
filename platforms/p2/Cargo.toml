[package]
name = "embsim-p2"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Propeller 2 platform for embsim — FFI trampolines + P2 constants"
keywords = ["embedded", "emulator", "propeller2", "sil"]
categories = ["embedded", "simulation"]

[dependencies]
embsim-core = { path = "../../core" }
embsim-peripherals = { path = "../../peripherals" }
embsim-runtime = { path = "../../runtime" }
tracing.workspace = true

[dev-dependencies]
rstest.workspace = true
# Test-only: raw socketpair/fcntl/read/write to drive the serial trampolines
# against a real file descriptor (set non-blocking so reads never hang).
libc.workspace = true
